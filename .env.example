# Image version (use a specific tag like v1.0.0)
IMW_VERSION=v1.0.0

# Port exposed on the host
APP_PORT=8501

# Persistent storage on host (must be writable by UID 10001)
WORKBENCH_DATA_LOCATION=/path/to/ml-workbench/data
